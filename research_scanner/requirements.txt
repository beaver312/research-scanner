# Research Scanner dependencies
# (Most are already installed for Scholars Terminal)

# Core (already in Scholars Terminal)
chromadb>=0.4.0
sentence-transformers>=2.2.0
fastapi>=0.100.0
uvicorn>=0.20.0

# Scheduling (new)
apscheduler>=3.10.0

# Optional: for PDF text extraction from papers
# PyPDF2>=3.0.0
# pdfplumber>=0.10.0
