# Requirements for Re-Indexing Scholar's Terminal
# Install with: pip install -r requirements_reindex.txt

# Core dependencies
chromadb==0.4.22
PyPDF2==3.0.1
markdown==3.5.1
tqdm==4.66.1

# NumPy compatibility fix (ChromaDB needs 1.x, not 2.0)
numpy<2.0.0

# Optional but recommended
python-magic==0.4.27  # Better file type detection (Windows: python-magic-bin)
ebooklib==0.18       # EPUB support (if needed)
